model Step {
    id          Int     @id @default(autoincrement())
    description String
    order       Int
    image       String?
    video       String?
    recipeId    Int
    recipe      Recipe  @relation(fields: [recipeId], references: [id])
}
